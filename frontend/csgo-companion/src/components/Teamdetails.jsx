import React from "react";

const Teamdetails = ({ leagues, teams, players }) => {
  // Access the props and use them in your component
  console.log(leagues);
  console.log(teams);
  console.log(players);

  // Render the component
  return (
    null
    // JSX for the component
  );
};

export default Teamdetails;







